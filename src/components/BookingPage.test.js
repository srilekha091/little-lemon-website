import { fireEvent, render, screen } from '@testing-library/react';
import BookingPage from './BookingPage';

describe('Booking Page', () => {
	test('User entered the first name', () => {
		const firstName = '';
		const handleSubmit = jest.fn();
	});
});
